# 実験結果・研究の現状

> 本作を実験基盤として、LLM が会話を伴う非完全情報ゲーム（人狼）をどのように
> プレイするかを観察・比較した記録。数値は現時点の**予備的な結果（小標本）**であり、
> 確証には戦数を増やす必要がある。

---

## 1. 研究基盤（構築済み）

| 要素 | 役割 |
|---|---|
| `recorder`（schema v2） | 全対戦を統一スキーマで記録。**web/LLM のリアルタイム対戦も記録**。種別(human/cpu/llm)・モデル名・行動タグ(intent/target/result/basis)・reasoning を保存 |
| `dev/batch_run.py` | 無人バッチ対戦。席構成や `--models` を指定してデータ生成・モデル比較 |
| `dev/analyze_games.py` | 対戦ログから評価指標を集計（**種別 × 役職 × モデル**の勝率、投票精度、情報役職のCO率、intent分布） |

この3点で「対戦を回す → 記録する → 指標で分析する」という研究ループが回る。

---

## 2. 評価指標

- **自陣営勝率**：そのプレイヤーの陣営が勝った割合
- **投票精度**：村人陣営の投票が、実際の人狼に入った割合
- **情報役職のCO率**：占い師・怪盗が結果をCOした割合
- **intent 分布**：発言の行動タグの内訳

---

## 3. ルールベースCP のベースライン（CP対CP）

同一配札で計測した現行システムの傾向：

| 指標 | 値 |
|---|---|
| 村人陣営 勝率 | 約 37% |
| 情報役職のCO率 | 約 96〜100% |
| 役職別勝率（占い師 / 怪盗 / 村人 / 人狼） | 約 52% / 68% / 49% / 37% |

- 勝敗は主に**投票のダイナミクス**（人狼2体の連携、村側の非収束）で決まり、
  推論エンジンの重みを変えても勝率はほぼ動かないことを確認済み。
- 場に人狼が0体（約5%）だと構造上ほぼ村敗北（ノーロンチ機構が無い）。

---

## 4. LLM の振る舞いと、2段階の介入（gpt-4o-mini）

LLM 1体 + CPU 4体の対戦で、プロンプト介入の効果を役職別指標で計測した。

| 段階 | LLM総合勝率 | 情報CO率 | 人狼役勝率 | 投票精度 |
|---|---|---|---|---|
| ベースライン | 25% | **0%** | — | 38.5% |
| ＋CO指示（介入①） | 30% | **100%** | 0%(0/5) | 53.8% |
| ＋人狼指示（介入②） | **48.6%** | 91% | **22%**(2/9) | 60% |

**介入①（情報役職に必ずCOさせる）**
`_co_directive`：占い師・怪盗が未COなら、実際の夜情報つきで「今この発言で必ず
COせよ」という最優先指示を挿入。→ CO率 **0%→100%**、投票精度 +15pt。

**介入②（人狼の立ち回り）**
`_wolf_directive`：人狼陣営に「正体を隠す・仲間を守る・偽CO・村人票を割る」を指示。
→ 人狼役 **0%→22%**、総合 **30%→48.6%**（＝CPと同水準）。

### 介入②後の役職別勝率（35戦）

| 役職 | CPU | LLM |
|---|---|---|
| 占い師 | 52% | 33% (n=6) |
| 怪盗 | 68% | 75% (n=8) |
| 村人 | 49% | 58% (n=12) |
| 人狼 | 37% | 22% (n=9) |

到達後は**村人・怪盗役でCPを上回る**一方、相対的に弱いのは**占い師役**。

---

## 5. モデル比較（デモ）

`--models` により席ごとに別モデルを割り当てて比較できる。予備デモ（14戦）：

| モデル | 総合勝率 | 投票精度 | CO率 |
|---|---|---|---|
| gpt-4o-mini | 35.7% | 46.2% | 60% |
| gpt-3.5-turbo | 35.7% | 44.4% | 100% |

※ 14戦は小標本のため予備的。同一指標でモデルを並べて比較できる基盤が動作することを示す。

---

## 6. 現時点での知見

- **小型LLMの弱点を役職単位で定量化**できた：①情報役職で結果をCOしない、
  ②人狼役で欺瞞できない。
- **的を絞ったプロンプト介入で、これらを個別に改善**できることを実証。
  2段階の介入で、LLM を「単純ルールAIの約半分の勝率」から「同水準」へ引き上げた。
- 「観察 → 仮説 → 介入 → 再計測」のループが機能する環境が整った。

> **注意**：各条件の戦数は 14〜35 と小さい。傾向は明確だが、確証には各条件
> 100〜200 戦程度が必要。

---

## 7. 今後の課題

**確証・弱点の潰し込み**
- 主要条件を各100〜200戦回して勝率・役職別・CO率を確定する。
- 相対的に弱い**占い師役**に的を絞った介入 → 再計測。

**比較実験**
- 複数モデル（gpt系 / Claude / Gemini 等）を各30〜50戦で横並び比較。
- 実験ごとに `data/実験名/` で分け、before/after を自動比較する。

**人間らしさ（もう一つの軸）**
- web対戦で人間の対戦ログを収集（記録基盤は完成済み）。
- 人間/LLM の発言を判別する「チューリング評価」で人間らしさを定量化。
- 人間ログを用いた few-shot / fine-tune。

**発展**
- 論拠グラフ（claim graph）による推論の高度化、CPのバランス・ルール拡張。

---

## 8. 再現方法

```bash
# ルールベースCPのバッチ（無料）＋記録
python -m dev.batch_run --games 200

# LLM 1体を入れて対戦（要 OPENROUTER_API_KEY）
python -m dev.batch_run --games 50 --llm 1 --out data/exp1/

# モデル比較
python -m dev.batch_run --games 50 --llm 2 --models "openai/gpt-4o-mini,openai/gpt-3.5-turbo" --out data/models/

# 指標を集計（現行 schema>=2 のみ。--dir で対象を指定）
python -m dev.analyze_games --dir data/exp1/
```
