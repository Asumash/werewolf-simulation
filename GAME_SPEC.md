# ワンナイト人狼 シミュレーション 完全仕様書

最終更新: 2026-06-08

---

## 目次

1. [ゲーム概要](#1-ゲーム概要)
2. [役職一覧](#2-役職一覧)
3. [ゲームの流れ](#3-ゲームの流れ)
4. [夜フェーズ詳細](#4-夜フェーズ詳細)
5. [議論フェーズ詳細](#5-議論フェーズ詳細)
6. [投票フェーズ詳細](#6-投票フェーズ詳細)
7. [勝敗判定](#7-勝敗判定)
8. [役職認識システム（_get_believed_role）](#8-役職認識システム)
9. [発言重要度スコアシステム](#9-発言重要度スコアシステム)
10. [CPの役職別発言内容](#10-cpの役職別発言内容)
11. [人狼の立ち回り戦略システム](#11-人狼の立ち回り戦略システム)
12. [矛盾検出システム](#12-矛盾検出システム)
13. [人狼確率（wolf_prob）計算システム](#13-人狼確率wolf_prob計算システム)
14. [投票ロジック詳細](#14-投票ロジック詳細)
15. [嘘判定システム（is_lie）](#15-嘘判定システム)
16. [発言解析パーサ](#16-発言解析パーサ)
17. [学習データの構造](#17-学習データの構造)
18. [統計データ](#18-統計データ)
19. [ルール変更履歴](#19-ルール変更履歴)

---

## 1. ゲーム概要

- **プレイヤー数**: 5人（Alice, Bob, Carol, Dave, Eve）
- **役職構成**: 人狼×2, 占い師×1, 怪盗×1, 村人×1 ＋ 墓地×2
- **総発言数**: 15発言（`total_statements = 15`）
- **勝利条件**:
  - 村人陣営（占い師・怪盗・村人）: 人狼を1人以上処刑する
  - 人狼陣営: 人狼を1人も処刑されない

### 役職の2段管理

プログラム内で役職を2種類の変数で管理する。

| 変数 | 内容 | 用途 |
|------|------|------|
| `original_role_map` | 配布時（夜フェーズ開始直後）の役職スナップショット | 元人狼の行動判定・人狼の仲間識別 |
| `role_map` | 怪盗交換後の現在の役職 | 勝敗判定・占い師の確認結果 |

---

## 2. 役職一覧

| 役職 | 陣営 | 夜行動 | 特徴 |
|------|------|--------|------|
| 人狼（WEREWOLF） | 人狼陣営 | なし | 2人存在。`original_role_map`で互いを把握 |
| 占い師（SEER） | 村人陣営 | 他1人 or 墓地を確認 | 確認対象の役職（交換前）が分かる |
| 怪盗（ROBBER） | 村人陣営 | 他1人と役職を交換 | 必ず交換を実行。交換後の自分の役職が分かる |
| 村人（VILLAGER） | 村人陣営 | なし | 特殊能力なし |

---

## 3. ゲームの流れ

```
セットアップ
  └ 役職をシャッフルして5人に配布。末尾2枚が墓地。
  └ original_role_map = role_map のスナップショットを保存
    ↓
夜フェーズ（NIGHT_ORDER順に処理）
  └ 処理順: 占い師 → 人狼（何もしない）→ 怪盗
    ↓
議論フェーズ（15ターン）
  └ 毎ターン全員のurgency_scoreを計算 → 重み付き抽選で話者1人選択 → 発言生成
    ↓
投票フェーズ
  └ 全員が同時にwolf_probを計算して投票先を決定
    ↓
勝敗判定
  └ 処刑者の role_map（交換後）で判定
```

---

## 4. 夜フェーズ詳細

### 処理順序（`NIGHT_ORDER`）
`占い師 → 人狼 → 怪盗`

占い師は怪盗より先に動くため、占い師が見た役職は必ず交換前の状態と等価。

### 占い師の夜行動

```python
if random.random() < 0.7:
    return {"action": "see_player", "target": random.choice(others)}  # 他プレイヤー1人を占う
return {"action": "see_graveyard"}  # 30%で墓地を見る
```

- プレイヤーを占った場合: `knowledge["saw_player"] = {target: role}` に格納
- 墓地を見た場合: `knowledge["saw_graveyard"] = [role1, role2]` に格納

### 怪盗の夜行動

```python
return {"action": "swap", "target": random.choice(others)}  # 必ず交換
```

- `role_map[怪盗]` と `role_map[target]` を入れ替える
- `knowledge["swapped_with"] = target`
- `knowledge["new_role"] = 交換後の自分の役職`
- **交換された相手には通知なし**

### 人狼の夜行動

- 何もしない（`{"action": "none"}`）
- 仲間の情報は `original_role_map` から事前に把握している

### 非対称情報まとめ

| 状況 | 知っている側 | 知らない側 |
|------|------------|---------|
| 怪盗が人狼と交換 | 怪盗（自分が人狼になったことを知る） | 元人狼（交換されたことを知らない） |
| 怪盗が村人と交換 | 怪盗（自分が村人になったことを知る） | 元村人（交換されたことを知らない） |
| 怪盗が占い師と交換 | 怪盗（自分が占い師になったことを知る） | 元占い師（交換されたことを知らない） |

---

## 5. 議論フェーズ詳細

### 話者選択の仕組み

毎ターン以下の手順で話者を1人選ぶ：

1. 全員の `get_urgency_score()` を計算
2. スコアを重みとした確率的抽選（`_weighted_choice`）で話者を決定
3. 選ばれた話者が `make_statement()` で発言を生成
4. 発言を `discussion_log` に追加
5. 15回繰り返す

### 発言重要度スコア（urgency_score）

#### 最優先: 矛盾指摘が可能な場合

```
矛盾指摘あり（人狼以外）→ 5.0（常に最優先）
```

#### 未発言の場合

| 役職 | 条件 | スコア |
|------|------|--------|
| 占い師 | — | **3.0** |
| 怪盗 | — | **2.5** |
| 人狼 | 現在の発言数 = 0 | **1.0** |
| 人狼 | 現在の発言数 ≥ 1 | **2.0** |
| 村人 | 現在の発言数 = 0 | **0.0**（発言しない） |
| 村人 | 現在の発言数 = N | **min(0.8, 0.08×N)** |

#### 発言済みの場合（M回発言済み）

```
decay = 1.2 × 0.55^M
村人の場合: decay × 0.8
最小値: max(0.15, decay)
```

| 発言回数M | 全役職 | 村人のみ |
|---------|-------|---------|
| 1回 | 0.66 | 0.53 |
| 2回 | 0.36 | 0.29 |
| 3回 | 0.20 | 0.16 |
| 4回 | 0.15（最小値） | 0.15（最小値） |

---

## 6. 投票フェーズ詳細

全員が同時に投票先を1人選ぶ。

### 村人陣営の投票

1. 占い師が「人狼」と確認した相手がいれば即決（`known_wolves`）
2. `wolf_prob`を `is_voting=True` で計算（発言数ペナルティ込み）
3. 矛盾指摘発言を参照してwolf_probを補正
4. wolf_prob最大のプレイヤーに投票

### 人狼の投票

1. `original_role_map` が人狼の仲間には投票しない
2. 村人陣営の発言ログから疑惑スコアを計算
   - 仲間・自分の発言は無視（偏りを避ける）
   - 疑惑発言キーワード: `「〇〇さんの発言が」「〇〇が怪しい」「〇〇は人狼」「〇〇を処刑」「〇〇に投票」`
3. 最も疑われているプレイヤーに便乗投票

---

## 7. 勝敗判定

```python
# 全員同票の場合
if len(executed) == len(players):
    winner = "village" if not wolves else "werewolf"

# 通常
wolf_executed = any(role_map[p] == WEREWOLF for p in executed)
winner = "village" if wolf_executed else "werewolf"
```

- 処刑者の **`role_map`（交換後の現在役職）** で判定
- 怪盗が人狼と交換された場合: その元怪盗（現在は人狼）が処刑されれば村人陣営の勝利

---

## 8. 役職認識システム

`_get_believed_role()` が各プレイヤーの「自分が信じている役職」を返す。

```
① knowledge に "new_role" がある（怪盗として行動した）場合
    new_role == 人狼 → WEREWOLF として行動（スワップを隠す）
    new_role == その他 → ROBBER として行動（スワップをCO）

② knowledge に "new_role" がない（交換された側）場合
    original_role_map == 人狼 の場合:
        議論ログに「自分と交換した」発言あり → 真の役職（ROBBER）に切り替え
        なければ → 引き続き WEREWOLF として行動
    それ以外 → original_role_map の役職をそのまま使用
```

### 各状況での行動

| 状況 | believed_role | 実際の行動 |
|------|-------------|---------|
| 元怪盗が人狼と交換 | WEREWOLF | 人狼として発言・投票 |
| 元怪盗が村人と交換 | ROBBER | 怪盗COして村人と発言 |
| 元人狼が怪盗と交換された | WEREWOLF | 知らないので人狼として行動し続ける |
| 元村人が怪盗と交換された | VILLAGER | 知らないので村人として行動（実態は怪盗） |

---

## 9. 発言重要度スコアシステム

セクション5に詳細記載。urgency_scoreの計算は `get_urgency_score()` が担当。

矛盾指摘の確認（`_try_expose_statement()`）は毎ターン全員分実行されるため、
矛盾が発生した瞬間から最優先スコア5.0が付与される。

---

## 10. CPの役職別発言内容

### 占い師の発言

**初回発言:**
- プレイヤーを占った場合（人狼）:
  > 「占い師です。〇〇さんを占ったところ人狼でした。信じてもらえると助かります。」
- プレイヤーを占った場合（非人狼）:
  > 「占い師です。〇〇さんを占いました。結果は△△でした。」
- 墓地を見た場合:
  > 「占い師です。墓地を見ました。['役職A', '役職B']でした。」
- 占い行動なし:
  > 「占い師ですが、有益な情報が得られませんでした。」

**2回目以降:**
- 人狼を確認していた場合: 確認済み人狼を言及しつつ、wolf_prob最大の別プレイヤーへの追加疑惑発言
- 非人狼を確認していた場合: wolf_prob最大プレイヤーへの疑惑発言
- 墓地確認の場合: wolf_prob最大プレイヤーへの疑惑発言

---

### 怪盗の発言

**初回発言（人狼以外と交換した場合）:**
> 「怪盗です。〇〇と交換しました。私は今△△です。」

**2回目以降:**
> 「〇〇と交換した結果を踏まえると、△△さんが最も怪しいです。」

**例外:** `knowledge["swapped_with"]` がない場合（元人狼が怪盗になった場合）は、
自分が怪盗だと気づかず村人として発言する。

---

### 村人の発言

**初回発言（議論ログが空の場合）:**
> 「〇〇さんはどんな役職でしたか？」

**通常発言（3パターンをローテーション）:**
- 「〇〇さんの発言が矛盾しているように感じます。」
- 「〇〇さん、もう少し詳しく説明してもらえますか？」
- 「私は〇〇さんが一番怪しいと思います。」

疑惑対象はwolf_prob最大のプレイヤー。すでに指摘済みの場合は次点へ。

---

### 人狼の発言

戦略（seer / robber / villager）に応じて発言。詳細はセクション11参照。

**初回発言（占い師偽装）:**
> 「私は占い師です。〇〇さんを占ったところ△△でした。」
- 占い結果: `["村人", "村人", "人狼"]` からランダム

**初回発言（怪盗偽装）:**
> 「怪盗です。〇〇さんと交換しました。私は今△△です。」
- 偽新役職: `["村人", "村人", "占い師"]` からランダム

**初回発言（村人潜伏）:**
> 「〇〇さんはどんな情報をお持ちですか？」

**2回目以降（全戦略共通の構造）:**
wolf_probが最大の非仲間プレイヤーを疑惑対象として、3パターンをローテーション。

---

## 11. 人狼の立ち回り戦略システム

### 戦略の決定フロー

```
過去発言に「占い師です」がある → "seer" を維持
過去発言に「怪盗です」+「交換しました」がある → "robber" を維持
どちらもない → _choose_wolf_strategy() で新たに決定
```

### 戦略選択の重み（`_choose_wolf_strategy`）

**デフォルト重み:**

| 戦略 | 重み |
|------|------|
| 占い師偽装（seer） | 0.70（70%） |
| 怪盗偽装（robber） | 0.30（30%） |
| 村人潜伏（villager） | 0.00（0%） |

**状況による重み変化（上から順に適用）:**

| 状況 | seer | robber | villager |
|------|------|--------|---------|
| 仲間が占いCO済み | 0.05 | 0.40 | 0.55 |
| 仲間が怪盗CO済み | 0.40 | 0.05 | 0.55 |
| 場に占いCO 2人以上 | ×0.20 | — | — |
| 場に怪盗CO 1人以上 | — | ×0.25 | — |

※複数条件が重なる場合は順番に適用される。最終的にsum=1に正規化。

---

## 12. 矛盾検出システム

### 検出の前提

- **人狼は矛盾を指摘しない**（`believed_role == WEREWOLF` の場合スキップ）
- 同じ指摘文（先頭30文字で照合）がすでにログにある場合はスキップ（重複防止）
- 矛盾を検出したターンの urgency_score = **5.0**（最優先）

### パターン1: 偽の占い師（確定）

**検出条件:**
- 2名以上が占いCOしている
- そのうち1人（claimer）がもう1人（target）を「人狼以外」と占い結果を出している

**ロジック:**
本物の占い師が別の占い師を「村人・占い師・怪盗」と見ることはありえない
（占い師は1人しかいないため）→ claimer が人狼と確定

**発言例（自分が本物占い師の場合）:**
> 「私が△△さんを村人と占いましたが、△△さんも占い師とCOしています。本物の占い師が別の占い師を村人と見ることはありません。△△さんが偽の占い師だと思います。△△さんへの投票を強く勧めます。」

**発言例（第三者が指摘する場合）:**
> 「〇〇さんは△△さんを村人と占いましたが、△△さんも占い師とCOしています。本物の占い師が別の占い師を村人と見ることはないため、〇〇さんが偽の占い師である可能性が非常に高いです。〇〇さんへの投票を強く勧めます。」

---

### パターン2: 偽の怪盗（確定）

**検出条件:**
- 2名以上が怪盗COしている
- そのうち1人（claimer）がもう1人（swap_target）と「交換して人狼以外になった」と主張している

**ロジック:**
怪盗は1人しかいない。swap_target が本物の怪盗なら claimer との交換は起きていない
→ claimer が人狼と確定

**発言例:**
> 「〇〇さんは△△さんと交換して村人になったと言っていますが、△△さん自身も怪盗とCOしています。△△さんが本物の怪盗なら〇〇さんとの交換は起きていないため、〇〇さんが偽の怪盗である可能性が非常に高いです。〇〇さんへの投票を強く勧めます。」

---

## 13. 人狼確率（wolf_prob）計算システム

`_calculate_wolf_probabilities(self_id, state, my_known_wolves, is_voting=False)`

### 初期値

| 対象 | 初期値 |
|------|--------|
| 全員 | 2 ÷ 5 = **0.4**（事前確率） |
| 自分（self_id） | **0.0**（常に除外） |
| known_wolves リスト | **1.0**（確定） |

### 変化条件一覧

#### 占いCO関連

| 条件 | 対象 | 変化量 |
|------|------|--------|
| 占いCOで「人狼」と判定されたターゲット | ターゲット | **+0.25** |
| 占いCOで「非人狼」と判定されたターゲット | ターゲット | **-0.25** |
| 占いCOが2名以上存在 | 全占いCO者 | **+0.15** |
| 同一ターゲットへ矛盾した占い結果（2人が異なる結果） | 両発言者 | **+0.5** |

#### 怪盗CO関連

| 条件 | 対象 | 変化量 |
|------|------|--------|
| 怪盗COで「今は人狼」と主張 | 発言者 | **+0.4** |
| 怪盗COが2名以上存在 | 全怪盗CO者 | **+0.15** |
| 怪盗COの交換先プレイヤー | 交換先 | **-0.2** |

#### 疑惑発言

| 条件 | 対象 | 変化量 |
|------|------|--------|
| 「〇〇さんの発言が」「〇〇が怪しい」「〇〇は人狼」 | 名指しされた人 | **+0.1 × (1 - 発言者wolf_prob)** |

発言者自身のwolf_probが高いほど影響が小さくなる。

#### 発言数関連

| 条件 | 適用タイミング | 対象 | 変化量 |
|------|-------------|------|--------|
| 0発言 | **投票開始時のみ** | 当該プレイヤー | **+0.35** |
| 1発言 | **投票開始時のみ** | 当該プレイヤー | **+0.20** |
| N発言（N≥1） | **随時（議論中も）** | 当該プレイヤー | **+0.005 × N×(N+1)/2** |

N発言時の合計上昇量の目安:

| 発言回数 | 合計上昇量 |
|---------|-----------|
| 1回 | +0.005 |
| 2回 | +0.015 |
| 3回 | +0.030 |
| 4回 | +0.050 |
| 5回 | +0.075 |
| 6回 | +0.105 |

#### 投票時の追加補正（`_vote_as_village` 内で適用）

| キーワード | 対象 | 変化量 |
|-----------|------|--------|
| 「〇〇さんへの投票を勧めます」 | 名指しされた人 | **+0.7** |
| 「〇〇さんのどちらかに投票」「〇〇さんへの投票を検討」 | 対象 | **+0.5** |

最終的に全値は **0.0〜1.0** にクリップ。

---

## 14. 投票ロジック詳細

### 村人陣営（`_vote_as_village`）

```
1. known_wolves があれば即決でそこへ投票
2. wolf_probs = _calculate_wolf_probabilities(is_voting=True) を計算
3. 矛盾指摘発言のキーワード検索で wolf_probs を補正
4. wolf_probs が最大のプレイヤーに投票
```

### 人狼（`_vote_as_werewolf`）

```
1. original_role_map が WEREWOLF の仲間は候補から除外
2. 村人陣営の発言ログから accusation_counts を集計
   （仲間・自分の発言は無視）
3. accusation_counts が最大のプレイヤーに便乗投票
```

---

## 15. 嘘判定システム

`_detect_lie(pid, statement)` が各発言の `is_lie` フラグを判定する。

```python
believed = _believed_role(pid, state)  # recorder.py の関数

# 「私は〇〇です」形式
if f"私は{role_name}" in statement and role_name != believed:
    return True

# 占い師CO
if "占い師です" in statement and "占い師" != believed:
    return True

# 怪盗CO
if "怪盗です" in statement and "交換しました" in statement and "怪盗" != believed:
    return True
```

`_believed_role`（recorder.py）のロジック:
- `knowledge["new_role"]` が人狼 → "人狼" を返す
- `knowledge["new_role"]` がその他 → "怪盗" を返す
- それ以外 → `original_role_map` の役職を返す

---

## 16. 発言解析パーサ

### `_parse_seer_claims(state)`

議論ログから占いCOの発言を解析し `(claimer, target, result)` のリストを返す。

**マッチ条件:**
- 発言に「占い師」が含まれる
- 「{other}を占」または「{other}さんを占」が含まれる
- 「結果は{役職}」「{役職}でした」「ところ{役職}」のいずれかが含まれる

### `_parse_robber_claims(state)`

怪盗COの発言を解析し `(claimer, swap_target, new_role)` のリストを返す。

**マッチ条件:**
- 発言に「怪盗」が含まれる
- 「{other}と交換」または「{other}さんと交換」が含まれる
- 「今{役職}」または「私は今{役職}」で新役職を取得（なければ None）

---

## 17. 学習データの構造

### ファイル

| ファイル | 内容 |
|---------|------|
| `data/game_XXXX.jsonl` | 個別ゲームログ（1ゲーム1ファイル） |
| `data/finetune_dataset.jsonl` | 全ゲーム統合のファインチューニング用データ |

※ `data/` フォルダは `.gitignore` で除外（ファイルサイズが大きいため）

### レコード種別

#### statement（発言）

```json
{
  "type": "statement",
  "player_id": "Alice",
  "role": "占い師",           // believed_role（発言者が信じている役職）
  "true_role": "占い師",      // 現在の実際の役職（交換後の role_map）
  "original_role": "占い師",  // 配布時の役職（original_role_map）
  "statement": "占い師です。...",
  "is_lie": false,
  "reasoning": "占い結果をCO。..."
}
```

#### vote（投票）

```json
{
  "type": "vote",
  "voter": "Alice",
  "voter_role": "占い師",      // 投票者の believed_role
  "voter_true_role": "占い師", // 投票者の現在役職
  "target": "Bob"
}
```

#### meta（試合情報）

```json
{
  "type": "meta",
  "game_id": "xxxxxxxx",
  "timestamp": "20260608_xxxxxx",
  "players": ["Alice", "Bob", "Carol", "Dave", "Eve"],
  "role_map": {"Alice": "占い師", ...},
  "original_role_map": {"Alice": "占い師", ...},
  "graveyard": ["村人", "人狼"],
  "result": {
    "executed": ["Bob"],
    "winner": "village"
  }
}
```

### roleフィールドの使い分け

| フィールド | 意味 | 活用場面 |
|-----------|------|---------|
| `role` | believed_role（発言動機に使う役職） | 発言の意図・戦略を学習する際 |
| `true_role` | 交換後の現在役職 | 客観的な正誤判定 |
| `original_role` | 配布時の役職 | 初期状態の把握 |

---

## 18. 統計データ

3,000ゲーム / 15発言時点のデータ。

### 勝率

| 陣営 | 勝率 |
|------|------|
| 村人陣営 | **50.6%** |
| 人狼陣営 | **49.4%** |

### 投票精度（人狼への投票割合）

| 役職 | 精度 |
|------|------|
| 怪盗 | 67.2% |
| 占い師 | 63.8% |
| 人狼 | 53.5% |
| 村人 | 7.9% |

### 人狼の戦術別勝率

| 戦略 | 人狼勝率 | 備考 |
|------|---------|------|
| 占い師偽装 | 45.1% | 矛盾検出されやすい |
| 怪盗偽装 | 70.1% | 検証が難しく有効 |
| 村人潜伏 | 61.2% | 発言数ペナルティで以前より弱体化 |
| 混合戦略 | 48.2% | バランスが取れている |

### 占い師のヒット率と勝率

| 状況 | 発生率 | 村人勝率 |
|------|--------|---------|
| 人狼をヒット | 14.3% | **72.0%** |
| 人狼を占わなかった | 57.4% | 41.3% |
| 占い師がCOしなかった | 28.3% | 51.6% |

### 矛盾指摘の発生率

| パターン | 発生率 |
|---------|--------|
| 全体 | 12.6% |
| パターン1（偽の占い師確定） | 9.3% |
| パターン2（偽の怪盗確定） | 3.3% |

### 初発言タイミング

| 役職 | 平均 | 中央値 | 主な分布 |
|------|------|--------|---------|
| 怪盗 | 1.8回目 | 1番目 | 1番目:55%, 2番目:24% |
| 占い師 | 2.1回目 | 2番目 | 1番目:46%, 2番目:26% |
| 人狼 | 2.7回目 | 2番目 | 1番目:23%, 2番目:32% |
| 村人 | 7.3回目 | 7番目 | 5〜9番目に集中 |

### 学習データ規模

| 項目 | 数値 |
|------|------|
| ゲーム数 | 3,000 |
| 学習サンプル数 | 45,000件 |

---

## 19. ルール変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-06-08 | 仕様書を完全版に全面改訂 |
| 2026-06-08 | 発言数ペナルティを投票時のみ（0発言+0.35/1発言+0.20）と随時（N発言+0.005×N×(N+1)/2）に分離 |
| 2026-06-08 | 占いで「人狼」と判定されたターゲットのwolf_prob変化を +0.5 → +0.25 に変更 |
| 2026-06-08 | 投票時に0発言+0.35 / 1発言+0.20 のペナルティを追加 |
| 2026-06-08 | 発言数に応じたwolf_prob微量上昇を追加（随時適用） |
| 2026-06-08 | 人狼の緊急度スコアを 0件=1.0 / 1件以上=2.0 に設定 |
| 2026-06-08 | 村人の初発言スコアを total=0 のとき 0.0 に設定 |
| 2026-06-08 | 人狼の戦略重みをデフォルト: 占い師70% / 怪盗30% / 村人0% に設定 |
| 2026-06-08 | 仲間が占いCO済み: 占い師5% / 怪盗40% / 村人55% に設定 |
| 2026-06-08 | 仲間が怪盗CO済み: 占い師40% / 怪盗5% / 村人55% に設定 |
| 2026-06-08 | 怪盗は必ず交換を実行するよう変更（スキップなし） |
| 2026-06-08 | 矛盾検出をパターン1（偽占い師確定）・パターン2（偽怪盗確定）の2種類に設定 |
| 2026-06-08 | total_statements を 15 に設定 |
