# CPUの動作仕様と改善案

> 人狼ゲームシステムにおけるルールベースCP（`players/rule_based.py`）の
> 現行動作の詳細と、人間/LLM混在を見据えた改善提案。
>
> 改善 **A・B・C・E は実装済み**。本書は実装後の現行動作を反映する。
> 関連ファイル: `players/rule_based.py`, `engine/game_runner_async.py`,
> `engine/game_state.py`, `engine/game_runner.py`

---

# 第1部：現在のCPUの動き（詳細）

## 0. 全体像

CPU（`RuleBasedCP`）の意思決定は4つ。推論の中核は **`BeliefState`（各プレイヤーの
人狼確率を導く単一の情報源）** で、発言・投票の全判断がこれを経由する。
各CPUは発言（Turn）到着ごとに増分更新される内部状態（`self._belief`）を保持する。

| 決定 | メソッド | タイミング |
|---|---|---|
| 夜行動 | `night_action` | 夜フェーズ |
| いつ話すか | `speak_urge` + ランナーの `_cpu_speak_loop`（イベント駆動） | 議論中 |
| 何を話すか | `make_reactive_statement` / `make_statement` | 発言時 |
| 誰に投票するか | `vote` → 陣営別メソッド | 投票フェーズ |

各CPUは生成時に**ペルソナ**を持つ（役職とは無関係で、タイミングからの役職バレを防ぐ）：

| ペルソナ | 値 | 役割 |
|---|---|---|
| `speed` | {0.7, 0.85, 1.0, 1.0, 1.3} | 反応の速さ倍率（小さいほど速い） |
| `talkativeness` | 0.8〜1.25 | 発言頻度倍率 |

---

## 1. 役職認識（`_get_believed_role`）

自分が「信じている役職」を返す。ここがCPUの行動の起点。

- **怪盗で人狼を引いた** → `WEREWOLF` として振る舞う（交換を隠す）
- **怪盗で人狼以外を引いた** → `ROBBER` のまま（交換をCOする）
- **交換された側（元人狼など）** → 交換通知がないので `original_role_map` のまま。
  議論で「自分と交換した」発言を見つけた場合のみ真役職に切替
  （怪盗は隠すため実際はほぼ発動しない）

## 2. 夜行動（`night_action`）

| 役職 | 行動 |
|---|---|
| 占い師 | 70%で他プレイヤーをランダム占い / 30%で墓地を見る |
| 怪盗 | **必ず**ランダムな相手と交換 |
| 人狼・村人 | なし |

## 3. いつ話すか（イベント駆動）

非同期ランナー `_cpu_speak_loop` が各CPUを独立ループで回す。

- ループは **0.8〜1.6秒** ごとにティックし、そのたびに `speak_urge` で
  「話したい度（urge）」と「理由（trigger）」を再評価する
- **反応トリガー**が立つと発話予定を前倒しし、会話の「返し」が成立する：

  | trigger | urge | 条件 | 発話予定の前倒し |
  |---|---|---|---|
  | expose | 5.0 | 矛盾を検出 | +1.5〜4s |
  | rebut | 4.2 | 対抗COが自分を名指し（カウンターCO最優先） | +1.5〜4s |
  | defend | 4.0 | 疑われた/名指しされた | +1.5〜4s |
  | rebut | 3.5 | 自分の夜知識と矛盾するCO | +1.5〜4s |
  | answer | 3.0 | 質問された（askタグ） | +1.5〜4s |
  | co | 2.5 | 情報役職・未CO | ― |
  | fill | 1.5 | 8秒以上沈黙 | +0〜2.5s |
  | open | 1.2 | 未発言 | ― |
  | base | 可変 | 発言済み（`0.6^回数 × talkativeness` で減衰） | ― |

- **初回発話予定**：役職に依存しないランダム初期遅延 `4〜16s × speed`。
  情報保持者（占い師・怪盗）のみ `-2〜6s` の前倒し（**決定的でない**ので役職バレしにくい）
- **発話後の次回予定**：`(12〜26s) × speed × (1 + 発言回数×0.25) / talkativeness`
- **クールダウン**：自分の直近発言から最低 5秒（defend/expose/rebut は 2.5秒）
- **低urge時**（urge < 1.0）：確率50%で見送り、無言の「間」を作る
- **反復回避**：直前の自分の発言と同一なら見送り

> `get_urgency_score` は同期（オフラインデータ生成）ランナー専用のレガシー。
> リアルタイムパスでは `speak_urge` に置き換わり不使用。

## 4. 何を話すか

リアルタイムパスでは trigger に応じて `make_reactive_statement` が文脈依存の発言を返す：

| trigger | 生成メソッド | 内容 |
|---|---|---|
| defend | `_say_defense` | 疑いへの反論（人狼は正体を隠し疑い返し／情報役職は役職を再主張） |
| answer | `_say_answer` | 質問への回答（役職を答える） |
| rebut | `_say_rebut` | 対抗COへの反論（カウンターCO） |
| expose | （`_try_expose_statement`） | 矛盾の告発文 |
| co / open / fill / base | `make_statement` に委譲 | 下記の通常生成 |

いずれも複数テンプレのプールから `_pick_fresh`（直近未使用を優先）で選び、反復を防ぐ。
ランナー側でも「直前の自分の発言と同一なら見送り」ガードがある。

### 通常生成（`make_statement`）
最優先で `_try_expose_statement`（矛盾指摘）をチェック。無ければ役職別に生成。
定型文は各役職で **5〜6種のプール** に拡張し `_pick_fresh` で選択。

#### 人狼（`_statement_as_werewolf`）
まず戦略を確率選択（`_get_wolf_strategy`）：

| 状況 | 占い偽装 | 怪盗偽装 | 村人潜伏 |
|---|---|---|---|
| 基本 | 0.70 | 0.30 | 0.00 |
| 仲間が占いCO済 | 0.05 | 0.40 | 0.55 |
| 仲間が怪盗CO済 | 0.40 | 0.05 | 0.55 |
| 場に占いCO2人以上 | ×0.20 | | |
| 場に怪盗CO1人以上 | | ×0.25 | |

- **占い偽装の初手**：ランダムな相手を占ったと偽CO。結果は `村人/村人/人狼`（=1/3で人狼と詐称）
- **怪盗偽装の初手**：ランダムな相手と交換し `村人/村人/占い師` になったと詐称
  （人狼になったとは言わない）
- **村人潜伏の初手**：仲間以外に「どんな情報を？」と話を振る
- **2回目以降**：仲間以外で人狼確率が最大の村人へ疑いを向ける（戦略別 5種プール）

#### 占い師（`_statement_as_seer`）
- 占い結果／墓地結果を初手でCO、2回目以降は結果を踏まえ次に怪しい相手を指摘（複数プール）

#### 怪盗（`_statement_as_robber`）
- 交換相手と交換後の役職を初手でCO、2回目以降は疑い発言（複数プール）
- 交換された側で知識が空 → 村人として発言

#### 村人（`_statement_as_villager`）
- ログが空なら役職を尋ねる
- それ以外は人狼確率が最大の相手（既指摘なら次点）を 6種プールで指摘

## 5. 矛盾検出（`_find_contradiction`、村人陣営のみ）

- **パターン1**：占いCO2名で片方が他方を「非人狼」と判定 → 判定した側が偽占い師
- **パターン2**：怪盗CO2名で片方が他方と交換し非人狼になったと主張 → その側が偽怪盗

検出すると「〇〇への投票を強く勧めます」まで含む強い告発文を出す。先頭30文字で重複排除。

## 6. 人狼確率モデル（`BeliefState`）— 推論の中核

**`BeliefState` が人狼確率の単一情報源**。発言（Turn）が届くたびに `observe()` で
生の証拠（占い/怪盗CO・疑い・発言数・投票誘導）を**増分蓄積**し、`probabilities()` が
蓄積済み証拠から一括で確率を算出する。「いつ／何を話すか」「誰に投票するか」の
全判断がこの単一モデルを経由する（＝LLM/学習方策に差し替える継ぎ目）。

- 各 `RuleBasedCP` は `self._belief` を保持し、`_get_belief(state)` が未処理Turnを取り込む
- 別ゲーム・ログ巻き戻しを検知して自動リセット
- 増分更新は全再計算と**完全一致**（検証済み）
- 旧 `_calculate_wolf_probabilities` は後方互換ラッパー（`BeliefState.from_state`）として存置

事前確率 = 人狼数/人数（= 2/5 = **0.4**）から加減算：

| 要因 | 増減 |
|---|---|
| 占いで「人狼」判定 / 「非人狼」判定 | +0.25 / −0.25 |
| 占いCO複数（各人） | +0.15 |
| 同一対象に矛盾する占い結果（各claimer） | +0.5 |
| 怪盗「人狼と交換」主張 | claimer +0.4 |
| 怪盗「非人狼と交換」主張 | claimer −0.1 / 交換相手 −0.2 |
| 怪盗CO複数（各人） | +0.15 |
| **疑う/投票誘導タグ** | +0.15 ×（発言者の信頼度）|
| **矛盾指摘タグ** | +0.20 ×（発言者の信頼度）|
| **擁護（vouch）タグ** | **−0.15 ×（発言者の信頼度）**（正の証拠）|
| 沈黙（投票時のみ、0発言/1発言） | +0.35 / +0.20 |
| おしゃべり | +0.005×n(n+1)/2 |
| 投票誘導シグナル（投票時のみ） | +0.7 / +0.5 |

※ 発言者の信頼度 = `1 − その人の人狼確率`（信頼できる人の指摘ほど重い）

※ 占い・怪盗の結果は intent タグ（`seer_result` / `robber_result` + target + result）を
最優先で読み取る。2回目以降の結果復唱は `suspect` タグになるため二重計上されない。

### 論拠（basis）の検証（改善D Lv1）
人間/LLM の「矛盾指摘」に **`basis`（根拠にした相手）** が付いている場合、
その矛盾が公開ログに**実在するか**を `_conflict_exists` で照合する：

- **実在**（basis内に競合するCOがある）→ 通常どおり対象を加算（採用）
- **実在しない**（ハッタリ/誤り）→ 加算を**無効化**し、**扇動した本人**の疑いを +0.10

擁護（vouch）は発言由来で初めての「正の証拠」。`basis` は CPU 発言には付かず、
**人間/LLM のみが供給**するため、CP対CPのバランスは不変（検証済み）。

## 7. 投票（`vote`）
- **村人陣営**：確定人狼がいれば即投票。無ければ投票モードの人狼確率が最大の相手へ
  （沈黙ペナルティ・投票誘導シグナルを含む）
- **人狼**：仲間には絶対投票せず、村人陣営から最も疑われている候補に便乗
  （※ここは意図的にテキストパターンのみで集計する。タグ対応にすると人狼が
  過度に有利になりバランスが崩れるため）

## 8. タグ推定（`infer_tags`）
CPU発言を `(intent, target, result)` に構造化し、人間・LLMと同じ行動言語に変換する。

| intent | 意味 | target | result |
|---|---|---|---|
| seer_result | 占い結果CO | 占った相手 | 人狼 / 村人陣営 |
| robber_result | 怪盗結果CO | 交換相手 | 交換後の役職 |
| suspect | 疑う | 対象 | ― |
| vote_suggest | 投票誘導 | 対象 | ― |
| contradict | 矛盾指摘 | 対象 | ― |
| vouch | 信頼（擁護） | 対象 | ― |
| ask | 質問 | 対象（任意） | ― |
| none | 通常発言 | ― | ― |

さらに `contradict` / `suspect` は **`basis`（根拠にした相手のリスト）** を持てる（§6で検証）。
CPU 発言には付与せず、人間UI（根拠の複数選択）・LLM API（`basis` フィールド）のみが供給する。

---

# 第2部：改善の進捗と残タスク

## ✅ 実装済み（A・B・C・E・D-Lv1）

| 改善 | 内容 | 反映箇所 |
|---|---|---|
| **A. イベント駆動の発言制御** | 固定タイマーを廃し、`speak_urge` による毎ティック再評価へ。疑われた/質問された/矛盾を見つけた等で発話を前倒しし「返し」が成立 | §3 |
| **B. 役職とタイミングの分離** | 初回発話を役職非依存の乱数＋ペルソナ `speed` に。情報保持者も非決定的な前倒しのみ → 順番での役職バレを抑制 | §3 |
| **C. 反復・ロボット感の除去** | テンプレを5〜6種に拡張し `_pick_fresh` で直近未使用を優先。宛先付きの反応発言（defend/answer/rebut）を追加 | §4 |
| **E. 意思決定コアの一本化** | `BeliefState` を新設し、発言・投票の全判断を単一モデルへ統合。投票側の重複解析を吸収。旧ロジックと完全一致（バランス中立）を検証済み | §6 |
| **D. 論拠の消費（Lv1）** | `Turn.basis` と `vouch` intent を追加。擁護（正の証拠）と、矛盾指摘の論拠検証（実在→採用／ハッタリ→無効化＋扇動者を疑う）を belief に実装。CPUは basis/vouch を出さないためCP対CPは不変 | §6・§8 |

## 🟡 残タスク（人間・LLM混在）

### D-Lv2/Lv3. 論拠の高度化（claim graph / 論証グラフ）
Lv1 は「相手参照＋矛盾検証」まで。Lv2 では各CO・疑いを**ノードID化**して
「主張#3に反論」のような参照を可能にし、Lv3 で重み付き論証グラフの収束計算へ。
LLM 本格導入時に拡張する。

### F. 情報公平性の明示・強制
CPUが使うのは「公開ログ + 自分の夜知識」のみ、と明示的に制約。
LLM（プロンプトで同じ情報を渡す）と **同一条件** で比較できるようにする。

## 🟢 残タスク（比較実験の質）

### G. 難易度・ペルソナのパラメータ化
攻撃性・正直さ・推論深度などのノブを外出しし、多様なCPU相手を生成 → LLMとA/B比較。
（`speed` / `talkativeness` は先行実装済み）

### H. 統計ペナルティの再調整
沈黙・おしゃべりペナルティは「全員が同程度話す」前提。人間はタイプ量が偏るため、
**発言率ベース**に変えるか、おしゃべりペナルティを撤廃。

### I. 学習データ用の統一トラジェクトリ記録
`(状態スナップショット, 選択可能な行動, 選んだ行動, belief)` を
human/CPU/LLM 共通スキーマで記録。タグ体系がその第一歩。

---

## 次に着手するなら

A・B・C・E・D-Lv1 は完了。`BeliefState` が単一の意思決定コアになり、
人間/LLM の論拠（basis）・擁護（vouch）も消費できるようになった。次は：

- **LLM プレイヤー**：`BeliefState` を継ぎ目に、確率算出や発言生成をLLMへ差し替え。
  LLM は `{intent, target, result, basis}` を返せば既存の消費経路にそのまま乗る
- **D-Lv2（claim graph）**：主張のノードID化。LLM本格導入と同時に
- **バランス調整（分析済み）**：同一配札で正しく計測した CP対CP の村勝率は **≒37%**。
  内訳は「場の人狼2体（47%）で村35%」「人狼1体（48%）で村47%」「人狼0体（5%）は
  ノーロンチ機構が無く村0%」。**belief の重みを振っても勝率はほぼ動かず**、勝敗は
  投票ダイナミクス（2体人狼の連携／村側の非収束）で決まると判明した。
  そのため重みの当て込みで数字を作ることはせず、**主要な重みを調整可能な定数
  （`W_SEER_WOLF` 等）として外出し**するに留めた。~50% への是正には、村側の投票収束を
  高めるロジック（例：真の占い師への信頼集約）やノーロンチ機構が必要で、これは別課題。
